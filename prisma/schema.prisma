// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://vercel-admin-user:rPEfaNMgEB0104X4@cluster0.6ry1c54.mongodb.net/devTibiantisClub?retryWrites=true&w=majority"
}

model PlayersHistory {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  quantity  Int
  createdAt DateTime @default(now())

  @@map("playersHistory")
}

model PlayersOnline {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  vocation  String
  level     Int
  createdAt DateTime @default(now())

  @@map("playersOnline")
}

model Foods {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  nutrition Int
  weight    Float
  imageSrc  String
  createdAt DateTime @default(now())

  @@map("foods")
}
